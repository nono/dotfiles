[Unit]
Description=Archive Claude Code prompt history daily
# ~/.claude/history.jsonl is rotated, so snapshot it more often than weekly.

[Timer]
OnCalendar=daily
Persistent=true

[Install]
WantedBy=timers.target
